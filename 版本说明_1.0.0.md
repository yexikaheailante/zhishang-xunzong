# 纸上寻踪 1.0.0

本版集中调整界面信息密度、常用网站和资料导入流程。导航结构、中央阅读区域、专注阅读、OCR、翻译、批注、记录、资料库格式与导出能力均保持原有边界。

## 本版调整

- 2026-09-07：现有 OCR 设置新增千问、Claude、自定义 API，支持两种图像接口协议与繁体/简体输出；框选及整页识别继续沿用原入口。
- 云端配置“已保存”与“已验证”明确区分；百度 API Key / Secret Key 错误提供可操作的提示。API 费用由用户选择的服务商收取。

- 隐藏无意义的 `0份`、`0条`、`0个` 与 `0 / 0`；分类和项目入口不再重复显示资料总数。
- 资料打开后仍显示当前位置；搜索、选择、导入结果、错误与冲突等行动相关数字继续显示。
- “常用网站”改为浏览器收藏夹式界面：文件夹横向排列，网址以紧凑卡片展示，不再保留“全部网站／最近使用／未分类”等额外分区。
- 添加与编辑网址的表单默认收起；文件夹支持双击改名，并可用行尾 `×` 经确认后删除。
- 导入支持把一个或多个文件、文件夹或混合项目直接拖入软件窗口。
- 点击导入时同时保留“选择文件”和“选择文件夹”。
- 导入流程缩短为“选择／拖入来源 → 核对清单并导入”；高级设置默认折叠，确认按钮本身即代表同意复制。
- 没有研究项目时，首次导入会自动建立“未归档资料”。
- 同名文件仍默认安全跳过，可在高级设置中改为自动编号；来源文件只复制，不移动、不删除。
- 悬浮阅读窗口支持直接修改正文、自动保存，并可用 `Ctrl+Z` 逐步撤销；OCR 原始文本与临时对照仍保持只读。
- 已移除响应速度不适合即时框选流程的“Codex（云端）”OCR 引擎；曾经保存的识别结果及来源记录不会被删除。

## 发布形式

本版只发布 Windows 10/11 x64 免安装客户端，不提供安装包。双击免安装 EXE 即可运行；用户资料库和本地设置不会因替换客户端而被主动删除。
